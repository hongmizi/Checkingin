<br /><br />
<div class="container">
  <div class="row-fluid">
    <div class="span6">
      <h3>你创建的项目:</h3>
      <% if @projects.blank? %>
        空
      <% else %>
        <ol>
          <% for project in @projects %>
            <li>
              <%= link_to(project.name, project_path(project.id),class:"")%>    </li> 
            <br />
          <% end %>
        </ol>
      <% end %>

    </div>
    <div class="span6">
      <h3>你参与的项目:</h3>
      <% if @join_projects.blank?%>
        空
      <%else%>
        <ol>
          <% for project in @join_projects %>
            <li>
              <%= link_to(project.name, project_path(project.id), class:"")%>
              <%= button_to( "今日打卡", checkins_path(:project_id => project.id), class:"btn btn-success")%>
            </li>
            <br />
          <% end %>
        </ol>
      <% end %>
    </div>
  </div>
