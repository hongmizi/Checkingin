<h1>New Project</h1>

<%= form_for @project do |f| %>
  <p>
    <%= f.text_field :name %>
  </p>
  <%= f.text_area :description, :row => 6 %>

  <div class = "actions">
    <%= f.submit :disable_with => "Submitting ..." %>
  </div>
<% end%>
